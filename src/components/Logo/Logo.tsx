import styles from "./styles.module.scss";

const Logo = () => {
  return (
    <div className={styles.Logo}>
      <h1 className={styles.title}>Revolutiorant</h1>
    </div>
  );
};

export default Logo;
